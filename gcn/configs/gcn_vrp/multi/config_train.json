{
  "max_epochs": 1500,
  "val_every": 10,
  "test_every": 2000,

  "batch_size": 20,
  "learning_rate": 0.001,
  "decay_rate": 0.99,

  "train_steps_per_epoch": 500,
  "test_finetuning_steps": 50,
  "test_finetuning_learning_rate": 0.0001,
  "test_after_every_finetuning_steps": 2
}
